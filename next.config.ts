import type { NextConfig } from "next";

// Semua gambar website ini lokal di folder `public` (foto brosur, foto menu,
// logo), jadi tidak ada host gambar luar yang perlu diizinkan di sini.
const nextConfig: NextConfig = {};

export default nextConfig;
